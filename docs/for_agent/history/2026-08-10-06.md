# 2026-08-10-06 · 策略仓位管理 2 条 memo（市值排序 + Slider 颜色）+ TDZ 崩溃修复

会话主题：处理策略仓位管理 2 条 open memo；修复排序改动引发的页面崩溃（TDZ）。

## 完成功能

1. **[策略仓位管理] "当前仓位"按当前市值排序（而非成本）**（msohisx9）
   - 根因：`mvOf` 用 `quantity × avgCost`（成本市值）排序
   - 修复：`mvOf` 改用当前市值（最新价 `pnl.byCode[code].latestPrice`，fallback 成本价）；`viewStocks` useMemo 依赖补 `pnl`（行情就绪后自动重排）
2. **[策略仓位管理] 数字滑动条增加颜色**（msohk4wc）
   - Slider（Base UI）：indicator `bg-blue-600`（主题蓝）、thumb `border-blue-500` + `ring-blue-500/30`、轨道 `bg-slate-200` 实色；playwright 实测填充/边框均为蓝色
3. **TDZ 崩溃修复**（验收前用户报"前端挂了"）
   - 根因：`mvOf` 引用后声明的 `marketValueOf`/`latestPriceOf`（mvOf@11612 < marketValueOf@12588）→ 运行期 `Cannot access 'marketValueOf' before initialization`（typecheck 查不出，函数体内引用）
   - 修复：`mvOf` 内联计算（`pnl?.byCode[code]?.latestPrice ?? avgCost`）；实测页面渲染 + 排序点击 0 JS 错误

## 验证
- typecheck 全绿；playwright（页面渲染 ✓ / 排序点击无错误 ✓ / Slider 蓝填充+蓝边 ✓）；trade-plan 定向冒烟 PASS

## Git
- `fix/memo-batch7`：c7ec321（2 文件 +9/-5）
- 合并 main：e928005 → **c7ec321**（fast-forward，验收通过）

## 遗留 / 规划
- 🚧 分支 fix/memo-batch7 合并后删除远程/本地分支
- 🔮 教训：前端改"引用位置靠后的函数"必须注意 TDZ（const 箭头函数声明顺序）——dev.md 已有运行期崩溃类教训，可并入 §7.3
