# 2026-08-05-27 — 处理改进备忘录 3 条（专题自选股 UI：名称列/拖拽排序/新建聚合）

**会话主题**：处理改进备忘录 3 条待处理项（均为专题自选股页面改进）。

## 处理内容

1. **msg0m8d8 名称列更大不换行**：表格列重构——「代码」「名称」两列合并为一列
   （名称大字 0.95rem + 代码小字 0.72rem 上下排布，whiteSpace: nowrap 不折叠），
   移除原固定序号列
2. **msg0ljk7 拖动排序（顺序=优先级）**：
   - 数据结构：WatchlistUpdateRequest 加 `reorderCodes`；store.updateTopic 支持重排
     （按 code 数组重排，未列出代码保持相对顺序补末尾）
   - 前端：表格行 draggable（⠿ 行头），HTML5 拖拽 onDragStart/Over/Drop，
     乐观更新 + 失败回滚；拖动时行高亮/虚线指示
3. **msg0jayp 新建专题聚合 UI**：左栏改为「➕ 新建专题」按钮 → 折叠展开聚合面板
   （蓝色底色边框区分于列表/导入区；名称+介绍+创建按钮聚合一处）

## 文件

- `shared`（+reorderCodes）、`features/watchlist/{store,index}.ts`、
  `web/tools/WatchlistTool.tsx`
- 分支 `feat/watchlist-ui`（等待验收）

## 验证

- ✅ typecheck 全绿；页面 200
- ✅ reorder API：商业航天专题倒序重排 → 恢复原序，均正确
- ✅ 3 条 memo 已流转 doing（验收后转 done）

## git

分支开发 → 验证 → 待用户验收 → 验收后合并 main
