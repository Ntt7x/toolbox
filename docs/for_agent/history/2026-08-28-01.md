# 2026-08-28 · 仓位管理 v2 备忘录批量 + 资金概览重构

## 背景
处理仓位管理 v2 的 5 条 open memo（mtcsk55o 聚合回溯 / mtco2den 隐藏列 / mtcop4jf 净值口径 / mtcrwxdk 日度汇总浮窗 / mtcorcho 单标的盈亏曲线）+ 用户追加的资金概览重构需求。

## 本次改动（main b1fc802，fast-forward 自 baa84a0）

### memo 处理
- **mtcsk55o 聚合回溯**：确认已满足——聚合分组分析实时聚合（"全部实盘"验证为聚合分组，实时算出 100 持仓/14 天净值/盈亏/归因全完整）→ done
- **mtco2den 隐藏列**：仓位明细隐藏"已实现/未实现"列（13 列→11 列，CSV 导出保留全列）
- **mtcop4jf 净值口径**：净值曲线双口径同时成图例（净值收益率% 橙实线 + 最大成本收益率% 绿虚线），删除切换按钮，口径说明合并展示
- **mtcrwxdk 日度汇总浮窗**：买入/卖出金额卡可点击（🔍 提示）→ 弹窗显示当日该方向流水
- **mtcorcho 单标的盈亏曲线**（重做）：服务端新接口 `GET /tools/trade-v2/groups/:id/stock-series?code=`（按 code 过滤条目 + buildDailySeries + 历史价市值）；下沉页"盈亏曲线（逐日 · 金额+收益率）"双线图（总盈亏金额 + 收益率%）

### 资金概览重构（用户追加）
- 资金概览块从收益分析 tab 移到**仓位明细 tab 顶部**（收益分析不再显示）
- 删除"资金概览"标题与说明
- "仓位"子块（今日加仓/剩余可用/累计净投入）删除，**嵌入"持仓"的"仓位比例"多行 sub**（StatItem 支持 string[]）
- 微调：仓位比例 sub 去"占总仓位"行；累计净投入移到持仓成本 sub；盈亏 StatGroup 总盈亏前置
- 净值曲线：去"组合净值"线改"累计盈亏"图例（= 市值 − 累计净投入）；默认不选中"持仓市值(成本)""最大成本收益率%"图例

## 验证
- typecheck（server+web）全绿；tradeV2 单测 40/40（含 capOverride）
- API 实测：stock-series（中油资本 14 天序列）
- 浏览器实测：下沉页盈亏曲线渲染、仓位明细顶部资金概览（3 卡 + 多行 sub）
- 冒烟 /tools/trade-v2 ALL-PASS

## 分支
`fix/memo-batch9`（98e5c59..b1fc802，8 commits）→ main fast-forward 合并 → 远程/本地分支已删
