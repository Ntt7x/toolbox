# 2026-08-29-02 · fix/tradev2-negprice 合并（允许负成交价）

## 需求
交易编辑/提交时允许负成交价（负成本摊薄场景，如卖出后成本转负），仅禁止 0 价；输入框移除 `min={0}` 限制。

## 改动
- `apps/web/src/tools/TradeV2Tool.tsx`：3 处
  - 校验 1：`draft.price <= 0` → `draft.price === 0`（提交校验，负价放行）
  - 校验 2：`!draft.price || draft.price <= 0` → `draft.price === 0`，提示由「价格必须大于 0」改为「价格不能为 0」
  - 输入框：移除 `type="number"` 的 `min={0}`，允许输入负值

## 验证
- L0 `toolbox typecheck` 通过（server + web）

## 流程
- 分支 fix/tradev2-negprice（d24ebcf，单 commit）→ 验收通过 → fast-forward 合并 main（ca6b4e3 → d24ebcf）→ push → 删本地分支（远程无该分支）

## 遗留
- 无
