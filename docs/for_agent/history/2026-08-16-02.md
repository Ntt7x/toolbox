# 2026-08-16-02 · 文档中心 vscode 布局 + 数学公式 + 前端经验沉淀

分支：`fix/docs-viewer`（18 提交，fast-forward 合并 main，cd790d0）

## 内容

### 文档中心 vscode 风格重构（核心）
- **tab 多开预览**：vscode 风格 tab 栏（中键/✕ 关闭、内容缓存、名截断）+ 内容区工具条（复制/去 VSCode/字号）
- **编辑器布局**：左侧文件夹树 + 右侧 tab+编辑器（非弹窗）；树含文档叶子（右键/拖拽/内联重命名/回收站入口）
- **数学公式**：katex 0.16.47 + singleDollarTextMath=false + 失败红字兜底；DeepSeek Chat Share 导入 md（chatToMarkdown）
- **TOC 目录**：左侧大纲（一级标题可折叠、点击跳转、内容区相对坐标高亮 + 600ms 跳转保护）；滚动容器 shadcn ScrollArea（修文本重叠）
- **右键菜单**：createPortal 到 body + 翻转贴近鼠标 + Escape 关闭（"太远"根因 = Math.min 压边）
- **minimap 移除**（用户决定）→ 设计入 plans/reading-minimap.md 暂缓
- **高度修复**：根布局 calc(100dvh - 56px) + overflow hidden + 独立滚动分区
- **其他**：非 LLM 标题提取（extractMdTitle）、侧边栏宽度 localStorage、文件重命名（PUT /docs/:id/rename）

### 文档治理
- **dev.md 大重构**：头部「⛔ 最深刻的教训 Top8」表 + §5.0/§5.4 压缩为指针 + §6 LLM 细节抽到 domains/llm.md（666→531 行）
- **新建 domains/frontend-experience.md**（前端全量经验 10 节：设计五问/组件选型/UI 细节/布局工程化/React 陷阱/踩坑集锦）
- **新建 domains/llm.md**（用量切面三标注/三模式/决策清单/JSON 容错）
- **组件化评估**：shadcn 官方+277 社区注册表无文件树组件；@toc-cn 网络超时；资源管理器保留手写
- **新增 shadcn 组件**：ScrollArea/Collapsible/DropdownMenu（Base UI 版）

## 关键教训（已入 dev.md）
- 手写交互组件优先成熟库（滚动/弹层/菜单/折叠一律 shadcn）——用户批"一堆 bug"
- 弹层 portal 到 body + 翻转贴近 + Escape；滚动容器 ScrollArea 防 flex 压缩重叠
- TOC 高亮用内容区相对坐标（视口绝对坐标在页面中部场景必错）
- 用户反馈≠实测时先自查验证脚本/HMR/窗口差异

## 验证
- typecheck 全绿；docs 单测 8/8；冒烟 18 页 ALL-PASS
- 交互实测：菜单翻转 2/2、TOC 高亮/折叠/收起 6/6、文档重命名 7/7、文件夹默认收起 5/5

## 状态
- 分支已删（远程+本地）；main = cd790d0 已 push
- 无 open memo
