# 2026-08-15-01 · 文档中心 feat/docs-center

## 范围
文档中心（工具分组）：markdown/pdf 管理与浏览 + Cordis 化 + 交互强化 + 折叠 + 数学公式。

## 提交
- `f0cf44b` feat(docs) 文档中心（13 文件 +1824）——合并 main（fast-forward）+ push + 删远程/本地分支

## 内容
1. **文档中心完整功能**（`apps/server/src/features/docs/` + `apps/web/src/tools/DocsTool.tsx`）：
   - md/pdf 上传（文件/文件夹 webkitdirectory 递归建文件夹）、文件夹树、tag 过滤
   - 知乎爬虫结果批量导入（复用 `zhihuCrawl:result:*`）
   - 回收站：软删（deletedAt）/恢复/整树恢复/彻底删除/清空
2. **Cordis 化**：4 个 Service（DocStore 存储 CRUD / DocFile pdf 文件 / DocIndex 搜索 / DocImport 导入），
   路由薄壳 + context.ts 异步单例 + plugin.ts 挂载（todoV3 同模式）
3. **交互强化**：文件夹/文档右键菜单、拖拽（文档→文件夹、文件夹→文件夹，禁止移入自身/子孙
   ——服务端权威环校验）、回收站视图
4. **文件夹折叠收起**（左栏箭头 ▶/▼，Set 状态）
5. **Markdown 数学公式**：MarkdownView 加 remark-math + rehype-katex（DeepSeek 网页版同款 KaTeX），
   `$...$` 行内 / `$$...$$` 块级；新依赖 remark-math/rehype-katex/katex

## 验证
- typecheck 全绿；docs 单测 5/5（含回收站：软删/恢复/级联/移动环拒绝）
- API 集成 13/13（_tmp_trash.mjs）；全量单测 154/154
- 冒烟 `/tools/docs` PASS；playwright 实测折叠 5/5 + KaTeX 公式渲染（3 元素 + 1 块级）
- 测试数据已清理；保留用户真实数据（64 篇文档 + 19 文件夹）

## 教训
- **Hono 静态路由被 `/:id` 吞（第三次）**：`/trash`、`/zhihu-results` 必须注册在参数路由前
- **docs:folders/docs:meta 的键是 `items`**（非 folders）：清理脚本按 `folders.folders` 会报错；
  清理生产 KV 前先看真实结构
- 上传含 `$` 的 md 内容时 remark-math 需与 rehype-katex 成对配置（remark 侧解析 → rehype 侧渲染）
