# 2026-08-22-01 · 仓位管理 v2 备忘录批（fix/tradev2-memo-b8 → main）

## 改动

**5 条 open memo 全部处理（仓位管理 v2）**：

1. **mt2ujmcq 总盈亏% 排序**：仓位表"总盈亏%"列可排序（按百分比数值）。
   - **TDZ 修复**：`costOf/totalPnlOf/totalPnlPctOf` 原定义在 `sorted` useMemo 之后，排序 `valOf` 引用报 "Cannot access before initialization" → 移到 `sorted` 之前。
2. **mt4hi1zc 空白输入补全已有标的**：提交交易单 + 批量交易单的标的输入框，空白时显示已有标的。
   - **补充（接口方案）**：前端不再过滤全账本，服务端新增 `GET /tools/trade-v2/groups/:id/stocks`（`stocksOfGroup`，去重 + 自动补名称），前端按当前分组（`draft.groupId` / 批量单 `groupId`）拉取——只显示当前分组标的。
3. **mt4hgp8b 标的限制移到下沉页**：标的历史下沉弹窗（移动分组下方）新增"该标的限制（占分组总仓位 %）"编辑保存。
4. **mt4hl5g9 信息分类 + 风险指标**：
   - 分组新增 `infoType`（"有信息"/"无信息"），服务端 store/services/路由 + 前端 GroupEditor 选择 + 贡献表徽章。
   - 收益分析新增风险指标：年化波动率 / 夏普比率 / 最大回撤（`metrics.ts` 纯函数 + 9 单测）。
5. **mt4hm8hp 数据工程深化**：指标计算纯函数化 `metrics.ts`（可单测）、数据源注册确认完整。

**用户补充**：
- 仓位管理页全部 23 个文本输入框加 `autoComplete="off"`（禁浏览器历史推荐）。
- 补全窗口高度 220 → 320（约 8 行）。

## 验证

- typecheck 全绿（server + web）
- tradeV2 单测 48/48（metrics 9 + tradeV2 39）
- 冒烟 `/tools/trade-v2` PASS
- 接口实测：`/groups/g-msudvewx-7vtonf/stocks` 返回 22 个标的带名称
- 浏览器实测：EntryEditor 编辑某组交易 → 补全只显示该组标的

## 备注

- 期间反复 2 次"没生效"排查：根因是①浏览器旧 bundle（重启 dev 解决）②批量交易单（OrderSheet）行内补全仍用全账本 `existingStocks`（已改接口方案）——**入口区分：提交交易单=EntryEditor，批量交易单=OrderSheet**。
- 用户曾叫停 3 个方向（OrderSheet 去补全 / GroupEditor detail 加载 / 宽度调整）→ 已回退，最终按"服务端接口取分组标的"方案落地。
