# 2026-08-14-01 · 全库逻辑 bug 批量修复（P1×1 / P2×17 / P3×约 20 项落地）

时间：2026-08-14 00:00 会话主题：全库逻辑审计后批量修复 + 文档/脚本重组（refactor/agent-docs-toolbox 分支）

## 背景

- 前置会话完成全库逻辑审计（6 路子代理 + 主代理通读），产出 P1×1 / P2×17 / P3×40+ 报告
- 用户确认执行修复，并要求修复过程同步维护文档/脚本/引导 Agent 的进化
- 修复前先完成「文档 + 脚本」重组（toolbox 统一入口、文档地图、命令速查、历史索引）

## 完成的修复（按批次）

### 批1：P1 知乎爬虫 limit 语义
- `zhihuCrawler/service.ts`：limit=0（契约「不限制/默认 0」）不再被钳成每类 1 条；crawlQuestionAnswers 与用户分支统一「0=不限制」语义（含滚动/翻页/进度文案）

### 批2：破坏性/数据一致性 P2
- `books/index.ts`：DELETE /favorites 仅「不传 id」才清空，非法 id 400（防误清空全部收藏）
- `knowledgeHub.ts` migrateInstance：只删已迁移成功的源条目（冲突跳过的保留，防数据丢失）；单测同步更新语义
- `localData/index.ts`：单条 GET/DELETE /entry 补 key 归属校验（startsWith source，与 PUT 一致，防越权读删）
- `knowledgeHub/index.ts`：conflict 参数服务端校验（skip/overwrite/merge 值域，非法 400 防静默覆盖写坏知识）
- `knowledge.ts`：实例前缀去重（LLM 提取模板自带 medical. 前缀时不再产生 medical.medical.x 双前缀）
- `jsonParse.ts`：LLM 输出数组根支持（按「首次出现的括号类型」选择对象/数组提取；原实现会把数组降级为第一个元素对象 → 知识导入静默 0 条）；`knowledge.ts` 兼容 {facts:[...]} 对象包装，均无法解析时改为报错

### 批3：并发竞态
- `chatSession.ts`：会话级串行队列（同会话并发 ask 排队，防读-await-写竞态丢一轮历史）
- `reasonix.ts`：会话级串行队列（防 promptListeners 互相覆盖/误删 + reasonix 侧并发限制）
- `useAsyncTask.ts`：任务身份校验（旧任务迟到响应/轮询结果不得覆盖新任务；taskIdRef 同步）

### 批4：缓存与超时
- `cbRate/index.ts`：缓存 key v3→v4——无显式 month 时按查询日隔离（「本月以来/今年以来」是进行时数据）；period=year+month 时 month 纳入 key（防碰撞）；单测同步
- 三个 LLM 搜索任务超时 5min→10min（cbRate/treasuryFx/reverseRepo daily；dev.md §6.3 搜索常态 8~10 分钟）
- `llm.ts` chatSearch 内部硬超时 180s→600s（防慢搜索提前 abort）
- `watchlist/index.ts` extend 提示词缓存 key 纳入 topic.updatedAt（增删股即失效）
- `watchlist/store.ts` listTopics 上限 500→5000（缓存键不再挤掉专题键）
- `gridPlan/history.ts` 摘要 perBuy 取 bal（均衡档）而非 styles 首键 rad；shared 契约注释同步

### 批5：P3 精选防御
- `llm.ts`/`prompts.ts`：设置值非字符串（本地数据页误编辑）防 TypeError 崩溃
- `kvStore.ts`：kvCount/kvListRaw LIKE 通配符转义（用户可控 source 参数防误匹配）
- `tasks.ts`：TTL 清理跳过 running 任务（防终态不归档 + SSE 卡死）
- `deepseekShare.ts`：message_id 缺失用下标兜底（防前端 React key 全 0 冲突）
- `reverseRepo`：refresh 任务创建后补写 state.taskId + 补 module；runMonthlyUpdate 失败抛错（任务 error 态而非恒 done）；LLM 未知期限丢弃而非静默改写 6M
- `watchlist/index.ts`：optimize-reason 对不在专题的 code 400（防注入任意字符串代码）
- `knowledge.ts`：kbAsk/kbImportFromChat 透传 signal（任务取消可中断 LLM）
- `TradePlanTool.tsx`：默认日期/月用本地时区格式化（toISOString UTC 凌晨错位一天）
- `GlobalFloating.tsx`：STATIC_PATHS 死路由 /admin/deps → /settings/arch-graph

## 文档/脚本/引导进化（同步完成）

- **脚本**：`toolbox.mjs` 统一入口（list/help/子命令转发）+ `typecheck.mjs`；scripts/README.md 双层结构
- **文档**：docs/for_agent/README.md 文档地图、commands.md 命令速查、history/INDEX.md 索引；根 README 双视角重写
- **引导**：AGENTS.md 精简为启动仪表盘；dev.md 头部导读 + 常用命令表换 toolbox 入口 + 新经验小节（见 dev.md §6.6/§6.7/§6.3/§6.5）

## 验证

- L0：`toolbox typecheck` 全仓 Done（server + web）
- L1：`toolbox test` 全量 14 文件 146 用例 0 失败（含新增 migrateInstance 语义、cbRate key v4 断言）
- jsonParse 数组根/对象根/facts 包装行为实测通过

## 遗留/规划（🔮）

- 🔮 审计报告中剩余 P3（约 20 项低危：dependencyGraph core 内部边、sse 心跳泄漏、zhihu 历史 id 错位、books 429 语义、newsCenter 空配置、knowledgeSession 死代码等）未修，按需推进
- 🚧 分支 refactor/agent-docs-toolbox：文档+脚本重组与修复均未提交（等待用户验收后提交）
- 🚧 修复涉及单测语义变更两处（migrateInstance 只删迁移成功的、cbRate key v4），均已同步测试
