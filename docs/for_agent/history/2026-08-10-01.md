# 2026-08-10-01 · Base UI 切换 + 策略仓位管理（负成本盈亏 + 收尾）

会话主题：shadcn 底层切换 Base UI；trade-plan 负成本盈亏展示改造；收尾清理与文档同步。

## 完成功能（按序）

1. **shadcn 底层切换 Radix → Base UI**（`feat/tradeplan-shadcn`，合并 main）
   - `components.json` style: new-york → **base-nova**（shadcn 官网 2026-07 起新项目默认 Base UI；底层选择在 style 而非 base 字段）
   - 11 组件（badge/button/card/dialog/input/label/select/slider/switch/table/tabs）用 Base UI 版重新生成；依赖 `+ @base-ui/react`、`- radix-ui`（-71 包）
   - API 差异适配：Slider 单值（sliderNum helper）、Select value 可 null、placeholder 拒 null；恢复被覆盖的自定义（slider 轨道/button outline）
   - 新建 `docs/for_agent/domains/shadcn.md`（选型依据/切换方法/API 差异表/使用规则）
2. **trade-plan 多轮修复**（合并前的分支提交）
   - 重复应用根因：PUT 固化基线改差值法 `rebasePositions`；3 新单测
   - 数量/金额混淆：`itemText` 统一「加仓 X 股 ≈ ¥Y」（历史/日历/总计划）
   - 自动保存统一 `patchStrategy`（防抖 400ms + 传最新 next），修复 setTimeout 闭包旧值
   - 左栏「总仓位概览」功能区（4 卡 + 总计划滚动展开）；列表卡/概览/持仓行优化
3. **负成本盈亏展示**（本轮，已提交 main）
   - avgCost<0（成本已回本）**显示盈亏金额、不显示盈亏比例**：attachPnl 返回 pnl + costNegative:true，计入 totalPnl/totalMv、negCount 计数、不参与 totalCost；存在负成本标的 `totalPnlPct: undefined`（前端「—」）
   - 列表接口附盈亏（totalMv/negCount/totalPnlPct）——列表卡「当前市值」改最新价（修复负成本显示负市值/虚高百分比）
   - 前端三处（列表卡/概览卡/持仓行）显示「盈/亏 ¥X（—）」
4. **收尾**：cmd 垃圾文件清理；dev.md §6.8 脚本教训（模板字符串替换失败→patch.mjs、execSync 假成功、原子写盘）；domains/features.md §4.6 v5 负成本规则

## 验证
- typecheck 全绿；tradePlan 单测 18/18；trade-plan 定向冒烟 PASS
- playwright 实测：负成本三处展示（+¥3,800.80（—）/ 盈 ¥1,960.40（—））、Base UI 交互（按钮/Slider/Select）正常
- 18 页冒烟 ALL-PASS（Base UI 切换后全站无回归）

## Git
- `feat/tradeplan-shadcn`：1d7250a（Base UI）→ 8d108ab（trade-plan 多轮修复）→ 9985fb1（测试规范+脚本）→ a7cd694（负成本+收尾）
- 合并 main：f0206e4 → **a7cd694**（fast-forward）

## 遗留 / 规划
- 🔮 其他页面（CbRate/GridPlan/Kelly/ReverseRepo/TreasuryFx 等）渐进迁移 shadcn（存量页面不强制重写，§5.4）
- 🚧 分支 feat/tradeplan-shadcn 合并后删除远程/本地分支
