# 2026-08-05 39 · 处理改进备忘录（书籍收藏 + DeepSeek Share 提取历史）

## 处理条目（2 条 open → done）
- `msg9v0n1` 书籍下载页面：新增书籍记录收藏功能（标记要下载的书籍）
- `msg8y6il` DeepSeek Share 提取页面：增加提取历史记录

## 改动
### 书籍收藏（books）
- 服务端 `features/books/service.ts`：`addFavorite/removeFavorite/listFavorites/clearFavorites`——KV `books:favorites`，上限 100 条，按 book id 去重（重复收藏提到最前）
- 路由：GET/POST `/tools/books/favorites`、DELETE（?id= 单删 / 无参清空）
- 前端 BookSearchTool：结果卡片「☆ 收藏 / ★ 已收藏」（橙色高亮）+「📌 收藏的书（N）」折叠列表（详情页打开/取消收藏/清空）

### DeepSeek Share 提取历史（deepseek-share）
- 服务端：提取**成功**自动记录 `{url, shareId, ts, messageCount}`——KV `deepseekShare:history` 上限 50 条（url 去重提前）；GET `/tools/deepseek-share/history`、DELETE 清空；注册数据源 `deepseekShare:`（历史记录）
- 前端 DeepSeekShareTool：「🕘 提取历史（N）」折叠区——shareId + 条数 + 时间，点击重新提取，清空按钮；提取成功自动刷新

## 验证
- typecheck 全绿
- 收藏：added=true → 列表含测试书 → ?id= 删除 removed=true → 0 条
- 提取历史：真实提取 rwd9kxge44hyjho3fi(4条) 自动记录 → 清空 ✓；数据源 deepseekShare: 1 条
- 页面 books / deepseek-share 200；2 条 memo 已标 done

## 分支
feat/memo-fixes-2（待验收）
