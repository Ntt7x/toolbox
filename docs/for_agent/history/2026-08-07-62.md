# 2026-08-07 分支归档：feat/memo-batch3（已合并 main，验收通过）

## 内容（3 条备忘录 + 浏览器自动化全站接入）
1. **专题自选股**：
   - 延续思考提示词按专题缓存（`watchlist:extend:`，TTL 2 年）+ 🔄 刷新（force）
   - 个股列表「行情」列涨跌幅排序切换（仅展示，不影响存储顺序；排序视图禁用拖拽）
   - 修复 listTopics Summary 缺 `group` 字段（分组此前写库成功但列表不展示）
2. **LLM 设置**：单日构成扇形图「按用量/按费用」切换；byModule/byDay 聚合补 completionTokens + costUsd/costCny
3. **浏览器自动化（重头）**：
   - 新增公共模块 `core/browser.ts`：findBrowser / sleep / launchPersistentContext（持久化 profile + 指纹伪装 + 启动失败按 `--user-data-dir` 杀残留 Chrome 进程自愈，杀进程不删 cookie）
   - 新增 feature `features/browserChat`：一键「去 Chat」= 打开 chat.deepseek.com → 开深度思考/智能搜索 → `keyboard.insertText` 填入（React 受控组件正解）→ 自动 Enter 发送；单实例防 profile 锁；未登录轮询等待登录；`bringToFront` 置前
   - 前端公共工具 `deepseekChat.ts`；**全站 6 处 Chat 按钮统一接入**（网格/央行利率/国债/凯利/逆回购/专题延续思考）
   - 提速 ~50%：固定 3.5s 等待 → 条件等待（1.2s 路由稳定 + 等 textarea）；开关并行；发送延迟 200+150ms（实测 5.3s → 2.6s）

## 关键踩坑（已写入 dev.md §4.5）
- DeepSeek 输入框 React 受控：fill() 静默失败、keyboard.type 长文本丢字 → **insertText 整段插入 + inputValue 校验**
- 开关状态属性是 **aria-pressed**（aria-checked 恒 null）
- **顺序陷阱**：先开关 → 再填入 → 发送前重聚焦输入框（点开关抢焦点）
- profile 锁：残留窗口/进程 → 重试按 profile 杀进程（wmic/taskkill，仅 Windows）

## 提交
- main: 8c6999c（docs 分支规则强化）→ dbfcc4a（fast-forward 合入）
- 分支 10 个提交：memobatch3 系列 + browserChat 实现/修复/优化

## 下一步建议
- 全站「去 Chat」已统一，后续新页面加 Chat 按钮一律用 `openDeepSeekChat`
- 知乎爬虫可迁移到 core/browser（当前自含 launchZhihuContext，能力等价，低优先级）
