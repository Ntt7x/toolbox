# 2026-08-05 33 · 处理改进备忘录（LLM 管理重构 + Chat 补充剪贴板）

## 处理条目（4 条 open → done）
- `msg5v0xt` LLM设置改名为「LLM 管理」，布局区分 私钥配置 / 用量管理 两大模块
- `msg5u3o7` 链接测试移动到 SK 输入框右边的并列按钮
- `msg5teca` 删除对话验证功能区
- `msg5p49o` 专题自选股 Chat 补充：新增获取剪切板自动填充能力

## 改动
- `apps/web/src/settings/LlmSettings.tsx` 重构：
  - 页面标题「🤖 LLM 管理（DeepSeek）」，desc 说明公共能力复用
  - 模块一「🔑 私钥配置」：状态徽章 + 输入行（输入框/显示/保存/清除/**测试连接**并列）+ 内联测试结果
  - 模块二「📊 用量管理」：平台余额（去充值）+ 本地汇总（总览/按模块/按天）
  - 删除对话验证（state prompt/chatting/chatReply/chatUsage + sendChat + UI 卡片）
  - 服务端 /api/llm/chat 保留（公共能力，UI 不再使用）
- `apps/web/src/App.tsx`：菜单「LLM 设置」→「LLM 管理」
- `apps/web/src/tools/WatchlistTool.tsx`：Chat 补充新增 `readAppendClipboard`（挂载自动捕获 + 📋 按钮手动），与 Chat 导入一致

## 验证
- typecheck 全绿（server+web）
- /settings/llm 200、/tools/watchlist 200
- 4 条 memo 已通过 PUT /api/tools/memo/:id 标 done

## 分支
feat/memo-fixes（待验收）
