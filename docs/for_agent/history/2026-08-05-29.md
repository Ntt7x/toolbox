# 2026-08-05-29 — 处理改进备忘录 3 条（LLM 用量监控/平台用量整合/触发原则）

**会话主题**：处理改进备忘录 3 条：
1) LLM 设置页引入 DeepSeek 平台用量数据整合
2) LLM 用量监控（服务端切面记录各模块用量）
3) 网站原则：禁止程序自动/隐式触发 LLM（dev.md 记录）

## 完成的功能

1. **#3 触发原则（先做）**：
   - dev.md §4.4 记录强制原则：程序不得主动/自动隐式触发 LLM，须用户主动操作或明确规划
   - **修复违规点**：reverse-repo GET /monthly 曾自动触发 LLM 月度更新 →
     改为仅手动 POST /monthly/refresh 触发（GET 只读，缺失仅提示 stale/staleMonths）
   - 审查其余调用点：cbRate/treasuryFx/watchlist/probeDaily 均用户点击触发 ✓
2. **#2 用量监控（服务端切面）**：
   - core/llm.ts：chat() 出口拦截，成功且有 usage 时记录 `llmUsage:log`（KV，2000 条截断）
   - ChatOptions 加 `module`；各调用方传归属（cb-rate/treasury-fx/watchlist.fundamental/
     watchlist.import/reverse-repo.daily/reverse-repo.monthly-update/llm.test）
   - `getLlmUsageSummary()`：总数 + 按模块 + 按天聚合
   - `GET /api/llm/usage` + `GET /api/llm/balance`（DeepSeek /user/balance，key 即授权）
   - 数据源注册 llmUsage:（运行状态）
3. **#1 平台用量整合**：
   - DeepSeek platform.deepseek.com/usage 网页需登录，无法程序化抓取明细；
     整合方案 = **本地切面记录为主 + 平台余额（/user/balance API）补充**
   - LLM 设置页新增「📊 LLM 用量监控」区块：平台余额（币种/总余额/充值/可用）+
     本地用量（总调用/tokens + 按模块表 + 按天表）

## 验证

- ✅ typecheck 全绿；页面 200
- ✅ 余额查询：CNY 21.73（充值 21.73）可用 true
- ✅ 用量记录：测试连接触发 2 次（llm.test:1次/103tok + 历史 unknown:1次/142tok），module 归属正确
- ✅ 数据源 llmUsage:（运行状态）注册

## git

分支 `feat/llm-usage`（等待验收）

## 下一步（验收后）

- 合并 main；删除远程分支；3 条 memo 标 done
