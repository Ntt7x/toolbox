# 2026-08-14-04 · 待办 v3（Cordis 框架）+ v1/v2 迁移整合

会话主题：待办事项从 v1 树状 → v2 自研 → v3（@deepseek-ai/cordis 框架）演进收尾；
v1 数据迁移 v3、清理 v1/v2 代码页面、文档/脚本进化。途中发生一次**数据丢失事故**并修复。

## 交付

1. **待办 v3（Cordis 框架）**：features/todoV3/
   - 三个 Cordis Service（store/scheduler/resolver）+ declare module + 异步 plugin 集成 Hono
   - 分解树（parentId）× 依赖（dependencies DAG）正交合并：
     组合环检测 / 级联完成+向上传播 / 级联删除×引用自愈 / 跨期递归重置 / 孤儿自愈
   - 页面树状折叠 + 进度条 + ＋子任务 + 依赖/周期徽标
2. **迁移整合**：v1 17 条真实待办 → v3（验证后删 v1 KV）；删除 v1/v2 代码页面；
   shared 类型统一 TodoItemV3
3. **数据安全修复（事故）**：
   - 事故根因：todoV3 单测 `finally { kvSet(KEY,{items:[]}) }` 清空生产 KV → 全量单测清掉
     迁移数据（20 条→0）——**单测操作真实 KV 禁止 finally 清空**
   - 修复：单测改 beforeEach 备份/afterEach 恢复（实测测试后数据完好）
   - 数据恢复：从对话历史重建 13 条可见待办（截断 4 条无法恢复，已向用户说明）
   - 新增 kv.mjs backup/restore（危险操作前先备份）
4. **文档/脚本**：domains/cordis.md（框架实践+踩坑+数据安全铁律）；dev.md §7.6；
   toolbox/README 同步

## 验证
- typecheck 全绿；全量单测 149/149；/tools/todo-v3 冒烟 PASS
- kv backup→模拟清空→restore 实测恢复成功
- 数据：todoV3:items 13 条 + 备份 .file/kv-backup/todoV3_items.json

## Git
- feat/todo-v2：cdca3f2（21 文件 +1197/-614）→ merge --no-ff → main
- 验收通过（用户 pass）

## 遗留 / 规划
- 🔮 截断的 4 条 v1 待办无法恢复（用户可见对话历史或手动补录）
- 🔮 其他 feature 单测若操作真实 KV 需检查是否也有 finally 清空（本次仅修 todoV3，
  全量扫描确认无其他清空操作）
