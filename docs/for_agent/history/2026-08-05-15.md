# 2026-08-05-15 — 本地数据治理：禁止硬编码 + 逆回购存量数据迁入 KV seed

**会话主题**：落实"所有本地数据统一到本地数据管理模块"原则——检查全库硬编码数据，
逆回购存量数据从代码直读改为 KV 幂等 seed；原则写入 dev.md。

## 完成的功能

1. **数据治理原则（dev.md §4.4）**：
   - 业务数据（表格/流水/配置/分析/探查结果）一律进「本地数据管理」（SQLite KV/表），
     禁止硬编码运行时数据；代码常量仅作"工厂默认值"，幂等 seed（KV 已有则跳过，不覆盖用户编辑）；
     用户可编辑/删除，删除后下次访问自动重新 seed（= 重置默认）
   - 例外：UI 文案、菜单结构、tools meta、纯计算参数
2. **逆回购存量数据 KV 化**（唯一硬编码业务数据，改造完成）：
   - `monthlyData.ts` 降级为"默认种子源"（注释警示），`service.ts` 新增
     `seedMonthlyData()` 幂等 seed 进 `reverseRepo:monthly`（source/operations/rows/seededAt），
     `getMonthlyData()` 运行时从 KV 读（空则 seed）
   - 数据源注册拆分：`reverseRepo:monthly`（存量数据）+ `reverseRepo:daily`（分析数据）
3. **顺带修复**：reverseRepo/index.ts 中 DAILY_CACHE_TTL_MS 缩进错乱

## 文件

- `features/reverseRepo/service.ts`、`index.ts`、`monthlyData.ts`
- `docs/for_agent/dev.md`（§4.4 + 逆回购条目更新）
- history 15（本文件）

## 验证

- ✅ typecheck 全绿
- ✅ KV 链路：GET monthly → 自动 seed（total 1、key reverseRepo:monthly）；
  删除 key → monthly 仍返回 23 行（自动重新 seed）；数据源列表显示两个 tag
- ✅ 提示词/prompts 已是同款 seed 模式（settings:prompt.*）

## git

本归档随改动一并提交并 push

## 遗留 / 规划

- 🔮 未来业务数据一律按本模式落地（新功能落地检查：数据可被用户编辑？可编辑即入库）
