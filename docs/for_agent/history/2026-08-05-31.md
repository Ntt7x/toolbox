# 2026-08-05-31 — 交易网格计划新增「历史网格计划」

**会话主题**：交易网格计划页面新增"历史网格计划"功能——保存生成记录
（时间戳 + 输入参数 + 完整结果），可查看/删除。

## 完成的功能

1. **数据结构**（features/gridPlan/history.ts，单 KV 文档 `gridPlan:history`）：
   - `GridPlanHistoryEntry`：id + createdAt（ISO 时间戳）+ request（type/boll/maxAmount）+
     summary（typeName/U/M/L/档数/maxAmount/perBuy 均衡档单档买入额）+ result（完整结果含 markdown）
   - 上限 50 条截断最旧；生成成功自动保存；数据源注册（存量数据）
2. **API**：
   - POST /tools/grid-plan 成功时自动 saveHistory
   - GET /tools/grid-plan/history（列表摘要，最新在前）
   - GET /tools/grid-plan/history/:id（详情，完整 result）
   - DELETE /tools/grid-plan/history/:id（删除）
3. **前端 GridPlanTool**：页面底部「📜 历史网格计划（N）」折叠区块——
   列表（时间/类型/布林带/档数/上限/单档买入 + 查看/删除）；
   查看复用 ResultView 渲染完整历史计划详情

## 验证

- ✅ typecheck 全绿；页面 200
- ✅ 端到端：生成 → 自动保存 1 条（单边强牛市 3 档，时间戳正确）→ 详情（markdown 1656 字）→
  删除 → 0 条；数据源 gridPlan:history（存量数据）

## git

分支 `feat/gridplan-history`（等待验收）

## 下一步（验收后）

- 合并 main；删除远程分支
