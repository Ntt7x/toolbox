# 2026-08-05-17 — 余额曲线升级 ECharts（x 轴精确到月/日期 + 逐笔投放柱）

**会话主题**：买断式逆回购存量余额曲线改用专业图表组件 ECharts，
横坐标精确到月甚至日期，叠加逐笔投放量展示。

## 完成的功能

1. **引入 echarts**（`pnpm --filter @toolbox/web add echarts`）
2. **新组件 `apps/web/src/components/BalanceChart.tsx`**：
   - 余额折线：月度累计净投放（= 存量余额），x 轴 time 轴精确到月；
     `expandMonths` 补全首尾间每月（缺失置 null + connectNulls:false 断开 2025-04~2026-02 断档）
   - 投放量柱：41 笔逐笔操作（精确到日期），右侧副轴（双 y 轴：余额/投放）
   - 交互：十字准星 tooltip、dataZoom 框选 + 滑块缩放、峰谷 markPoint、渐变面积、
     万亿单位格式化
3. **ReverseRepoTool**：删除手写 SVG 折线图，替换为 `<BalanceChart series operations />`

## 文件

- `apps/web/src/components/BalanceChart.tsx`（新）、`apps/web/package.json`（+echarts）、
  `pnpm-lock.yaml`、`tools/ReverseRepoTool.tsx`
- docs 待 history 17

## 验证

- ✅ typecheck 全绿（server+web）；页面 200
- ✅ API 数据：series 12 点 / operations 41 笔（2024-10-28 ~ 2026-08-05）

## git

本归档随改动一并提交并 push

## 遗留 / 规划

- 🔮 其余工具页（如 treasury-fx）如需要更丰富图表可复用 echarts 组件模式
