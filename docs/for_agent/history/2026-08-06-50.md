# 2026-08-06 50 · 处理改进备忘录（6 项全清：任务历史 + 任务信息 + 备忘录分类）

## 需求
用户"处理备忘录"，6 项 open：
- msh3tevf 备忘录分类（修复型/需求型）+ dev.md 规则
- msh3kyg0 / msh3k2bq / msh3jtol 三个页面新增历史任务功能
- msh3jgd4 / msh3i4aa 国债/央行分析页面新增任务信息

## 实现
### 任务历史持久化（服务端 core/tasks.ts + sse.ts）
- `createTask(opts.module)`：提供 module 的任务在**终态**（done/error/cancelled/超时）统一 `finalizeTask` 归档到 KV `taskHistory:<module>`（保留最近 50 条）
- `getTask` 增 `finishedAt/durationMs/module`（任务信息）
- 新 API：`GET /api/tasks/history?module=`（列表，新的在前）、`GET /api/tasks/history/:taskId`（详情，直接扫 KV 前缀，不依赖内存）
- 三处业务任务传 module：cb-rate / treasury-fx / reverse-repo.daily
- 数据源注册 `taskHistory:`（本地数据管理可见，tag 运行记录）

### 前端
- 通用组件 `components/TaskHistory.tsx`：状态徽章（成功/失败/取消/运行）/ 时间 / 耗时，点击展开结果（renderResult 由页面传入），刷新按钮
- cbRate：历史 + 运行计时（`已耗时 Xs`）+ CbRateHistoryView（复用 BankCard）
- treasuryFx：历史（复用 ResultView）+ 运行计时
- reverseRepo：历史探查任务（内联视图）+ 探查按钮计时
- api.ts：taskHistoryList / taskHistoryEntry；TaskHistoryEntry/TaskHistoryListResponse 契约

### 备忘录分类（msh3tevf）
- shared：`MemoKind = "fix" | "feature"`；MemoItem.kind
- store/index：createItem(text, kind) / updateItem 支持 kind；POST /tools/memo 接受 kind
- 前端 MemoTool：新增时类型选择 chips（🔧修复型/🧩需求型 + 说明）；条目类型徽章
- **dev.md §8.0 规则**：开发者驱动 Agent 默认只完成修复型（fix）；需求型（feature）需用户显式确认

## 验证
- typecheck 全绿；单测 49/49
- API 实测：seed 后 `GET /api/tasks/history?module=cb-rate` ok:true total:1；`GET /api/tasks/history/t1` 返回条目（修复 getTaskHistoryEntry 只查内存的缺陷 → 改扫 KV 前缀）
- 备忘录 6 项已标 done（含 kind 分类），剩余 open：无

## 分支
feat/memo-taskhistory（ff2aa13，待验收）
