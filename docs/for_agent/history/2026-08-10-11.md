# 2026-08-10-11 · 「不常用」菜单分组（默认收起）

会话主题：菜单分组新增「不常用」（默认收起），收纳低频页面。

## 完成功能

1. **「不常用」分组**（msqb197d）：后台移除本地数据/架构图；工具移除 books/knowledge-hub；新增「不常用」（staticItems：架构图/本地数据 + toolIds：知识库中心/书籍下载）
2. **默认收起**：collapsed 初始「不常用」= localStorage 无记录时 true（用户展开后记忆）
3. **旧菜单顺序安全**：resolveGroupItems 自动过滤不属于当前分组的条目（无重复渲染）
4. **冒烟修复**：arch-graph 断言改「项目架构依赖图」（分组收起后侧边栏不再含"架构图"文本——菜单结构调整影响冒烟断言的教训）

## 验证
- typecheck 全绿；playwright 实测默认收起（▸ 不常用 (4)）+ 展开 4 条目齐全；arch-graph/books 冒烟 PASS

## Git
- `fix/memo-batch10`：a49b4c1（2 文件 +15/-7）
- 合并 main：8d8162d → **a49b4c1**（fast-forward，验收通过）

## 遗留 / 规划
- 🚧 分支 fix/memo-batch10 合并后删除远程/本地分支
- 🔮 菜单分组结构调整后：若冒烟断言依赖侧边栏文本，需同步更新（本次 arch-graph 已踩）
