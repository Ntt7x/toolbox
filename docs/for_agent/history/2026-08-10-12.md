# 2026-08-10-12 · 待办清单服务端逻辑补全（孤儿自愈 + 树状联动 + 周期循环）

会话主题：用户报「待办清单数据链路和代码逻辑有 bug」，并追问服务端逻辑、周期循环逻辑——全面补全 store 数据层。

## 完成功能

1. **孤儿自愈**（数据链路 bug）：清空已完成删父后，未完成子任务 parentId 悬空 → 前端不显示
   - `load()` 读时自愈（悬空提升顶层 + 持久化，任何路径产生的孤儿都收敛）
   - `clearDone` 先删后自愈（未完成子立即提升，不丢失）
   - `addTodo` MAX_ITEMS 截断后自愈
2. **树状完成联动**（服务端逻辑）：完成父 → 级联完成全部子孙；子全完成 → 父自动完成（向上传播）；取消子 → 向上取消父
3. **周期循环重构**（原"done:false 重置"缺陷：无完成反馈 / 周期子 done 恒 false 致父无法自动完成 / 无跨期语义）
   - `TodoItem.lastDoneAt`；完成 = done:true + 记录
   - `listTodos` 跨期自动视为待做（daily=今日 0 点 / weekly=本周一 / monthly=本月 1 号）
   - 前端绿色徽标「✓ 本期已完成（MM-DD）」

## 验证
- typecheck 全绿；todo 冒烟 PASS
- API 实测：孤儿自愈（清空后子提升顶层）✓；树状联动 3 场景（级联/向上传播/取消传播）✓；
  周期 4 场景（完成记录/级联/本期/跨期重置）✓；测试数据已清理（用户 12 条完好）

## Git
- `fix/memo-batch11`：c927b59（3 文件 +111/-18）
- 合并 main：bbcb3f3 → **c927b59**（fast-forward，验收通过）

## 遗留 / 规划
- 🚧 分支 fix/memo-batch11 合并后删除远程/本地分支
- 🔮 周期 todo 后续可扩展：到期提醒/通知（当前只在列表中体现"待做"）
