# 2026-08-06 48 · 处理改进备忘录（4 项 open 全清）

## 需求
处理改进备忘录中的 4 项 open 问题。

## 实现
### 1. LLM 用量按调用模式统计（msh2i77q）
- shared：新增 `LlmCallMode = "direct" | "chat-session" | "reasonix"`；`LlmUsageSummary.total` 增 `byMode`（含中文 label）
- core/llm.ts：`ChatOptions.mode?`（默认 direct）；`UsageEntry.mode`；`recordLlmUsage(..., mode)`；`getLlmUsageSummary` 按模式聚合（旧数据无 mode 按 direct）
- 三模式切面适配：chatSessionAsk 传 `mode: "chat-session"`；reasonixAsk 记录 `mode: "reasonix"`；chat() 直调默认 direct
- 前端 LlmSettings：用量区块新增"按调用模式"列（三栏：模式/模块/天）
- 测试：llm.test.ts 新增模式聚合用例（中文 label + cacheRate）

### 2. 自选股雪球跳转（msh2o3zh）
- 前端 `stockDetailUrl(code, kind)`：股票/ETF → `xueqiu.com/S/<SH|SZ|HK+code>`；场外基金 → `fund.eastmoney.com/<code>.html`；北交所/无法识别 → 雪球搜索
- 个股表名称/代码列改为可点击链接（代码蓝色下划线 + ↗），新标签打开
- 服务端行情公共模块已有腾讯多源快照（行情/名称/PE/PB/市值），财报分析走 LLM 专题能力——满足"行情/基础信息/财报"整理

### 3. 专题今日行情速览（msh2lspq）
- WatchlistTool 专题详情顶部：上涨/平盘/下跌家数 + 平均涨跌幅（红涨绿跌）+ 快照更新时间（基金净值不计入）

### 4. 本地数据管理存储 size（msgb0zol）
- shared：`LocalDataSource.sizeBytes?`、`LocalDataEntry.size?`
- 服务端：sources 接口对 KV 源扫描求总字节（UTF-8）；entries 每条带 size（表条目取 JSON 序列化字节）
- 前端 LocalData：源列表显示 `N 条 · X KB/MB`；条目表新增"大小"列（B/KB 自适应）

## 验证
- typecheck 全绿；单测 47/47（新增 llm mode 聚合）
- API 实测：sources 18 个源带 sizeBytes（settings 37.5KB / cbRate 164KB 等）；`/api/llm/usage` byMode = direct 20 次 · 中文 label · cacheRate 73.3%
- 旧用量数据（无 mode）兼容按 direct 统计

## 分支
feat/memo-toolbox（f63ee9a，待验收）
