# 2026-08-10-09 · 3 条 memo（新闻分页 / 周期 todo / 仓位默认降序）

会话主题：处理新闻中心、待办清单、策略仓位管理共 3 条 open memo。

## 完成功能

1. **新闻中心分页获取更多**（msq32kgv）
   - 东财源 `fetch(page)`（URL 页码 `getlist_102_ajaxResult_50_<page>_`）；`fetchNews(sourceIds, page)`
   - 缓存 key 按源+页码隔离（`news:items:<id>:p<page>`，防分页污染）；路由 `?page=N`（默认 1）
   - 前端：首屏第 1 页 →「⬇ 加载更多（第 N 页）」→ 按 url 去重追加；`hasMore` 单页满 40 判断
   - 实测：page1/page2 各 50 条、跨页 0 重复
2. **待办清单周期 todo**（msq2mrqy）
   - `TodoItem.repeat?: "daily"|"weekly"|"monthly"`（shared + store normalize/updateTodo + PUT 路由）
   - 前端每行周期下拉（无/每日/每周/每月，周期项蓝色高亮）；**勾选 = 完成本次自动重置**（done 保持 false + updatedAt 刷新 + 提示）
3. **仓位列表默认按市值降序**（msq2zrzs）：`sortMode` 初始 `"none"` → `"desc"`（「默认」按钮仍可切回存储顺序）

## 验证
- typecheck 全绿；news-center / todo / trade-plan 冒烟 PASS
- 集成实测：分页独立无重复、周期设置+重置、测试数据已清理（9 条全为用户真实待办）

## Git
- `fix/memo-batch9`：166f27c（8 文件 +106/-22）
- 合并 main：81ac1a0 → **166f27c**（fast-forward，验收通过）

## 遗留 / 规划
- 🚧 分支 fix/memo-batch9 合并后删除远程/本地分支
- 🔮 周期 todo 可扩展：记录"上次完成时间"与"下次到期"（当前只刷新 updatedAt，用户未要求）
