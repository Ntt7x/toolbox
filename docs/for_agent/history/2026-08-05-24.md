# 2026-08-05-24 — 改进备忘录第 1 条：规范化 Agent Git 分支流程（dev.md）

**会话主题**：处理改进备忘录条目「规范化 Agent 开发的 Git 流程：
每次修改新建分支 → 分支验证 → 用户验收通过后合并 main」。
本会话本身即按新流程执行（分支 chore/git-flow-doc 上修改）。

## 完成的内容

1. **dev.md §6 重构**：
   - §6.1 分支工作流（强制，2026-08-05 起）：每次修改必须 `git switch -c <type>/<简述>`，
     分支上验证（typecheck/单测/API/页面）→ commit → push 分支；
     **用户验收通过后**才能 merge main；紧急修复经用户明确要求除外；
     新 Agent 开工前检查当前分支与待验收分支
   - §6.2 提交与推送：分支上 `git add -A && commit && push -u origin <分支>`；
     push 状态确认 `## <分支>...origin/<分支>`
2. **改进备忘录条目** msfzvliz-bb8tac 状态流转 doing（待用户验收后转 done）

## 文件

- `docs/for_agent/dev.md`（§6 重构）、`docs/for_agent/history/2026-08-05-24.md`
- 分支：`chore/git-flow-doc`（未合并 main，等待验收）

## 验证

- dev.md 为纯文档改动；无代码/测试影响
- 分支已 push 到 origin/chore/git-flow-doc

## 下一步（用户验收后）

- 用户确认 → `git switch main && git merge chore/git-flow-doc && git push origin main`
  → 删除远程分支 → 备忘录条目标 done
