# 2026-08-16-01 · 仓位管理 v2 UI 批量修复（fix/tradev2-ui-batch）

## 范围
处理 5 条仓位管理 v2 UI 备忘录：Select 名称显示、市值排序、占比百分比、颜色区分、弹窗滚动。

## 提交
- `2166d32` fix(trade-v2)（1 文件 +26/-17）——合并 main（fast-forward）+ push + 删远程/本地分支

## 内容
1. **Select 显式显示名称/中文（memo msuu343m）**：**根因 = Base UI 的 Select.Value 渲染
   value 本身（id）而非选项文本**（shadcn 切 Base UI 后的全局隐患）——trade-v2 全部
   6 处 SelectValue 改为显式映射（分组名、买入/卖出、全部分组/全部操作）
2. **仓位明细默认按市值降序（msuu4cw4）**：sortKey 初始 = marketValue + asc=false
3. **占总仓位列（msuu26al）**：全局视图也显示（本地按 Σ|市值| 算占比）；CSV 导出加列
4. **颜色区分度（msuu6upt）**：市值列负值绿/正值深色加粗；盈亏/未实现% 加粗；占比>100% 琥珀预警
5. **分组设置弹窗滚动（msuu1gub）**：DialogContent max-h-[85vh] overflow；标的列表独立滚动容器

## 验证
- typecheck 全绿；冒烟 PASS
- playwright 两轮实测：分组名称 ✅ / 市值降序（38.7→6.6→4.6）✅ / 占总仓位 ✅ / 未实现% ✅ /
  市值颜色 ✅ / 弹窗滚动容器 ✅
- 5 条 memo done

## 教训/待办
- **Base UI 切 Select 的全局隐患**：所有用 shadcn Select 的页面（todo-v3/docs/知识库等）
  的 SelectValue 都可能显示 value 而非文本——本次只修 trade-v2，**其他页面待全局排查**（可放改进备忘录）
- playwright 属性选择器 `[style*='maxHeight']` 匹配不到 React 渲染的 `max-height`（kebab-case）——
  验证脚本需用 `[style*='max-height']`
