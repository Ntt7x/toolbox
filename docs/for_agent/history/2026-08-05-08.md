# 2026-08-05-08 — 搜索注记改造成可编辑设置项（提示词链路 100% 一致）

**会话主题**：把 cbRate 的联网/知识搜索注记从代码常量改为可编辑提示词设置项，
消除「半一致」遗留，服务端对 LLM 的使用完全走同一提示词链路。

## 完成的功能

1. **prompts 注册表新增 2 个设置项**：`cb-rate.note.search`（联网搜索注记）、
   `cb-rate.note.knowledge`（知识模式防幻觉注记）——默认值 = 原代码常量，
   运行时可编辑可重置（prompts API / 本地数据管理页）。
2. **service 改造**：buildSystemPrompt 的 `{searchNote}` 替换改为
   `getPromptTemplate("cb-rate.note.search"|"cb-rate.note.knowledge")` 从库读取。
3. **渲染预览同步**：renderCbRateSystem 同样从库读注记——服务端实际拼装与页面
   rendered 预览完全同源（编辑注记后 `GET /api/prompts/cb-rate.system` 的 rendered 立即变化）。
4. 移除 `CB_RATE_SEARCH_NOTE_*` 导出（转为注册表默认值）。

## 文件

- `apps/server/src/core/prompts.ts`（注册表 + 渲染）
- `apps/server/src/features/cbRate/service.ts`（注记改库读）
- `docs/for_agent/dev.md`（§4 注记可编辑说明）

## 验证

- ✅ typecheck 全绿；单测 20/20；页面 200
- ✅ 链路验证：PUT 更新 note.search → `cb-rate.system` rendered 立即含新文本
  → reset → rendered 恢复默认「联网搜索说明」
- ✅ prompts 列表现有 5 个提示词（system/user/note.search/note.knowledge/grid-plan）

## git

本归档随改动一并提交并 push

## 遗留 / 规划

- 无（服务端唯一业务 LLM 调用 cbRate 的提示词链路已 100% 一致；
  通用 /api/llm/chat 为透传接口、testConnection 为 ping，均不适用注册表）
