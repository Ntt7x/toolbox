# 2026-08-05-06 — 央行利率分析页提供程序性提示词

**会话主题**：仿「交易网格计划」页，为「央行利率分析」页整理并提供「程序性提示词」查看/复制。

## 完成的功能

1. **提示词单一来源**（关键设计）：system prompt 模板从 service.ts 内联字符串抽到
   `packages/shared` 常量——`CB_RATE_BANKS_TEXT` / `CB_RATE_SEARCH_NOTE_DEFAULT` /
   `CB_RATE_SEARCH_NOTE_KNOWLEDGE` / `CB_RATE_SYSTEM_PROMPT_TEMPLATE`（占位符
   `{banksText} {calendarJson} {searchNote} {calendarRule}`）/ `CB_RATE_SYSTEM_PROMPT`（默认完整版）。
   server 的 `buildSystemPrompt` 改为模板替换（search/calendar 四组合），web 页面展示同一常量。
2. **编号统一**：原提示词 searchNote 动态编号与 action/calendar/banks 的 8/9/10 编号错位，
   重构为「要求 1-3 + 注记段落 + 规则列表」结构，任意组合编号不再冲突。
3. **页面 UI**：CbRateTool 参数区下方新增「📜 查看程序性提示词」卡片（仿 GridPlanTool）——
   展开 CodeBlock 展示默认完整版 + 「📋 复制」按钮。

## 文件

- `packages/shared/src/index.ts`：新增 CB_RATE_* 提示词常量（单一来源）
- `apps/server/src/features/cbRate/service.ts`：buildSystemPrompt 改用共享模板
- `apps/web/src/tools/CbRateTool.tsx`：提示词查看/复制卡片
- `docs/for_agent/dev.md`：§4 补充「程序性提示词单一来源」约定

## 验证

- ✅ typecheck 全绿（server+web）
- ✅ 单测 20/20
- ✅ 真实调用（知识模式 BOJ）：新模板下输出正常解析，dataMode=knowledge、asOf=2025-06-30、
  防幻觉注记生效（summary 自动注明知识截止日期）
- ✅ 页面 200

## git

本归档将随「程序性提示词」改动一并提交并 push

## 遗留 / 规划

- 🔮 gridPlan 的 GRID_PLAN_PROMPT 在 web 侧（gridPrompt.ts），因 server 是纯计算无需发送；
  若将来 grid-plan 也走 LLM，可同样上移 shared
