# 2026-08-05-09 — 提示词展示统一「查看提示词」+ Chat 跳转按钮

**会话主题**：统一两页面提示词展示文案，并增加携带提示词跳转 DeepSeek 网页版 Chat 的按钮。

## 完成的功能

1. **文案统一**：CbRateTool / GridPlanTool 的「查看程序性提示词/查看原始提示词」统一为
   「📜 查看提示词」（收起态「🙈 收起提示词」）。
2. **💬 Chat 按钮**（展开态，与「复制」并列）：点击后：
   - 先把提示词复制到剪贴板（双保险）；
   - 短文本（UTF-8 ≤2000 字节）用 `https://chat.deepseek.com/?q=<encoded>` 预填输入框；
   - 超长（grid-plan 全文 6280 字符中文约 17KB、cb-rate rendered 也超限）退化为
     打开空聊天页 + 粘贴，页面提示「若输入框未预填，请直接粘贴」。
3. 提示词区块文案同步说明「统一存储于本地设置数据，可编辑与重置」。

## 文件

- `apps/web/src/tools/CbRateTool.tsx`、`apps/web/src/tools/GridPlanTool.tsx`
  （openChat 函数 + chatHint 提示 + 按钮改造）

## 验证

- ✅ typecheck 全绿（server+web）
- ✅ 页面 200（cb-rate / grid-plan）

## git

本归档随改动一并提交并 push

## 遗留 / 规划

- 🔮 DeepSeek 网页版 `?q=` 预填为尽力而为（外部站点行为，无法服务端验证）；
  已用「复制+提示粘贴」双保险兜底
