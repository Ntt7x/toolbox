# 2026-08-19-01 · fix/memo-b7 合并归档

## 摘要
15 条 memo（两批：7 + 4）+ browserChat 连续点击窗口复用/CDP/防抖 + dev.md 教训沉淀。main 从 `fcc188c` fast-forward 到 `47541f0`。

## 提交内容（main=47541f0，13 文件 +238/-41）

### 第一批 memo（7 条）
1. **港股链接**：`xueqiuUrl` 港股去 HK 前缀（`/S/00189`）
2. **新闻中心字号**：标题 0.85→1rem、时间/来源 0.72→0.82rem（+15~18%）
3. **日度汇总净买/净卖**：动态"净卖出（卖−买）/净买入（买−卖）"
4. **仓位进度条**：仓位明细列表上方（总市值/总仓位上限），<60% 绿/<85% 琥珀/≥85% 红
5. **todoV3 归档**：仅顶级自动归档，子 todo 跟随父归档（自动+手动+恢复均递归子孙）
6. **成本口径统一**：`buildGlobalAnalysis` 跨组合按 code 合并 positions（摊薄 costAvg 口径），前端全部视图用 `global.positions`
7. **分组合并**：宏观利差 12 条 → 债汇组（31 条），删利差组（备份 165 条 KV 于 .file/kv-backup/）

### 第二批 memo（4 条）
8. **新闻中心 Chat 按钮**：每行 💬 → `openDeepSeekChat`（标题+摘要+来源+时间）；后移到日期后面
9. **标的列表按总盈亏排序**：新增可排序"总盈亏"列（已实现+未实现）
10. **日度汇总置顶**：交易流水 tab 移到最上方
11. **标的移动分组**：服务端 `POST /tools/trade-v2/move-stock` + 标的历史弹窗"移动到分组"

### browserChat（用户"打开 chat 改善"）
12. **CDP 优先**：探测 `--remote-debugging-port=9222` → `connectOverCDP` 复用已开浏览器（新 tab，不覆盖用户页面）
13. **窗口复用**（核心修复）：不再每次 close 旧窗口；activeCtx 存 **globalThis**（tsx watch 模块重载后仍保留）；复用同 tab 或新建
14. **前端防抖**：连续点击「去 Chat」忽略重复（提示等待）
15. **服务端 busy 等待**：连续点击排队（最多 30s）后复用窗口

### dev.md 教训沉淀（§7.2）
跨请求状态禁模块级 let（存 globalThis）/ 重构旧逻辑彻底删 / dev restart 假成功（kill-port 确认）/ 服务端调试用 API debug 字段

## 验证
- typecheck 全绿；tradeV2 39/39、todoV3 7/7；冒烟 /tools/trade-v2、/tools/news-center ALL-PASS
- browserChat 复用实测：第 1 次新开（debug=activeCtx-null）→ 第 2 次 **reused=true debug=reuse**（复用成功）；debug 字段验收后已清理
- 分组合并 API 验证：4 分组，债汇 31 条；global.positions 99 个摊薄口径

## 状态
- 分支 fix/memo-b7 已合并并删除（远程+本地）；open memo 0 条
- 遗留：browserChat 跨 tsx 全量重启（进程重启）仍无法复用窗口（无调试端口时）——属开发环境限制，生产无热更新不受影响
