# 2026-08-19-01 · trade-v2 标的补全下拉遮挡修复（portal+定位跟随+翻转）

## 摘要
修复仓位管理 v2 提交交易单时「标的代码/名称自动补全下拉被遮挡」问题。main `34d1fd7` fast-forward 到 `c7dc083`，memo `mszz1n74-4vveuz` 标记 done。

## 提交内容（main=c7dc083，1 文件 +38/-5，分支 fix/tradev2-autocomplete-portal 已删）
`StockSearchInput` 补全下拉：
1. **createPortal 到 document.body + position fixed + zIndex 9999**——完全脱离对话框/表格的 overflow/transform 裁剪链（前端经验 §2.3 规范落地）
2. **按输入框视口坐标定位**：`useLayoutEffect` 测量 wrapRef 的 getBoundingClientRect（修复 WIP 遗留：wrapRef 未挂到包裹 div，pos 恒 null 导致下拉固定在视口 (0,0)）
3. **滚动/缩放跟随**：window scroll(capture)+resize 监听实时重算
4. **空间不足向上翻转**：输入框下方放不下 220px 建议列表且上方有空间时翻转到上方

## 验证
- L0 typecheck（web）全绿；L2 冒烟 /tools/trade-v2 ALL-PASS
- playwright 实测 12 项全过：下拉为 body 直接子级/fixed/z-9999/含条目/水平对齐输入框/贴合输入框下方/完整在视口内/滚动 300px 仍跟随/小视口下方空间不足时向上翻转/EntryEditor 弹窗内同样 portal 定位且高于遮罩
- 数据链路确认正常（"平安"→8 条建议，WatchlistTool 同 API）；上轮「suggestionCount 0」实为下拉被遮挡测量误判

## 状态
- 分支已合并删除（本地+远程）；memo done；遗留无

## 教训
- 上一个会话遗留的 WIP（portal 改造）缺了关键一环：**声明了 ref 但没挂到元素上，定位状态永远不更新**——改 UI 后必须用浏览器实测坐标断言，不能只看代码
- 翻转测试要构造真实空间不足场景（缩小视口+滚动到指定位置），否则条件不触发会误判
