# 2026-08-10-02 · 处理备忘录 batch（trade-plan 买入价/卖出价 + watchlist 行情兜底与平均涨幅）

会话主题：处理 3 条 open fix 备忘录（msnj33y7 / msmu2uuw / msmu0jmj），分支 `fix/memo-batch-0810`。

## 完成功能（按序）

1. **[策略仓位管理] 加仓输入买入价、减仓输入卖出价 + 应用业务逻辑梳理**（msnj33y7-bygb76）
   - shared：`TradePlanItem.cost` 语义 = 本次交易价格（加仓=买入价、减仓=卖出价，>0，必填）；`TradePlanAfterPosition` 增 `tradePrice`
   - 服务端：`parseItems`（导出）宽松整形 + 新增导出 `itemsPriceError`（路由层权威校验，缺价 → 400「XX 的操作缺少买入价/卖出价」；check 与 day 两路由接入）；纯函数层（applyItems/checkTradePlan）保留三源兜底（存量历史计划/直接调用兼容）；missingPrice 告警文案改「未设置本次买入价/卖出价」；after 表填 `tradePrice`
   - 前端：输入行加仓「买入价（必填）」/减仓「卖出价（必填）」输入框（placeholder 随 action 切换，StepInput 支持 title 透传）；保存按钮禁用 + title 提示缺价；金额估算文案「请填买入价/卖出价」；itemText 显示 `@ ¥价格`；ResultView 增「操作价」列
   - 单测 +6（parseItems 合法/空、itemsPriceError 买入价/卖出价/≤0/合法），旧 2 断言文案同步，26/26 绿
   - 追加（用户追问「减仓不考虑卖出价吗」）：**减仓已实现盈亏** `realizedPnl` = (卖出价 − 当前持仓均价) × 减仓数量——shared `TradePlanAfterPosition.realizedPnl`、checkTradePlan after 表计算（存 day.result 快照，历史「查看」可见）、前端 ResultView 增「实现盈亏」列（红盈绿亏）；单测 +3，29/29 绿；API 断言：159206 减仓 100 股 @1.6 → realizedPnl 23.6、均价不变
   - API 断言：缺买入价 400、缺卖出价 400、带价 200（addTotal=数量×买入价、均价重算、tradePrice 填充）
2. **[专题自选股] 行情多数据源兜底**（msmu2uuw-ysuryl）
   - quote.ts：`fetchTencentBatch` 逐行容错（单只解析失败不再拖垮整批）；`getQuoteSnapshots` 批量命中无价（price 0/缺失）也降级单源补价
   - fund.ts：场外基金天天基金主源 → **新浪兜底**（`hq.sinajs.cn/list=of{code}`，GBK+Referer；字段 名称/单位净值/累计净值/昨净值/日增长率/日期），实测解析正确（161725 → nav 0.5781 / pct 2.46）
   - 前端：loadQuotes 保留失败项，行情单元格「⚠️ 无行情」+ title 失败原因（不再静默 —）
3. **[专题自选股] 专题列表显示当日平均加权涨幅**（msmu0jmj-cprc3h，用户选定**等权平均**口径）
   - shared：`WatchlistSummary` 增 `avgPct/avgCount`
   - 服务端：`GET /tools/watchlist` 改 async，`attachAvgPct` 收集全部专题股票/基金 → 分 40 只一批批量行情（复用 quote:s: 5 分钟缓存）→ 每专题有行情股票涨跌幅算术平均（normCode+裸码双键索引兼容用户代码写法）
   - 前端：列表行「N 只」旁红涨绿跌涨幅徽章（title 显示参与统计数）

## 验证
- L0 typecheck 全绿；L1 全量单测 14 文件全绿（tradePlan 26/26）
- API 断言：trade-plan check 缺买入价/卖出价 400、带价 200；watchlist 列表 avgPct/avgCount 正常（「通胀消费」36 只/32 有行情 +1.98%）
- L3 全量冒烟 18 页 ALL-PASS（core/quote+fund 改动触发全量）
- playwright 实测：加仓行「买入价（必填）」、切减仓后「卖出价（必填）」；watchlist 列表「36 只+1.98%」等徽章
- 无测试数据残留（check 为 previewOnly 不入库；列表接口只读）

## Git
- 分支 `fix/memo-batch-0810`（工作区未提交，等用户验收）

## 遗留 / 规划
- 🔮 策略级「已实现盈亏累计」（跨多次减仓汇总到策略总盈亏）暂未做——当前 realizedPnl 为单次操作级展示（result 快照），如需策略级累计再做
- 🔮 其他页面渐进迁移 shadcn（存量不强制，§5.4）
