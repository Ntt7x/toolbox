# 2026-08-19-02 · fix/browserchat-tab 合并归档

## 摘要
browserChat 复用窗口不覆盖已有页面 + 雪球 ETF 代码映射修正。main 从 `82bbcfd` fast-forward 到 `ddcbd6f`。

## 提交内容（main=ddcbd6f，2 文件 +6/-5）
1. `889e1e4` fix(browser-chat)：**复用窗口时不再覆盖已有页面**——原逻辑用 `pages()[0]`（窗口第一个 tab）goto 覆盖（把用户开着的页面顶掉）；改为复用/CDP 时优先用 `about:blank` 空 tab（避免空 tab 累积），否则新建 tab；仅首次新开窗口用初始空页
2. `ddcbd6f` fix(xueqiu)：**雪球 ETF 代码映射**——`[0135]\d{5}→SZ` 误把 5 开头沪市 ETF 归深市；拆为 `5/6→SH`（512800→SH512800）、`0/1/3→SZ`（159745→SZ159745）；验证 8 个代表代码（含港股裸代码/北交所 BJ）全部正确

## 验证
- typecheck 全绿；xueqiuUrl 映射 node 验证 8 例全部正确

## 状态
- 分支 fix/browserchat-tab 已合并并删除（远程+本地）；open memo 0 条
- main = ddcbd6f 已 push
