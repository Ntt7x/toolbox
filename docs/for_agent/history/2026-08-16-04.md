# 2026-08-16-04 · 备忘录 8 条（trade-v2 大批量 + docs 主题 + 知乎上限）

## 分支

- `fix/memo-tradev2-docs`（`423623a` + `70e4d5b`，fast-forward 合并 main `5391b29..70e4d5b`，远程/本地分支已删）

## 改动

### 第一批（423623a，3 条 + 净值重构）

1. **交易批量录入增强（msvvn2v4）**：OrderSheet 新增「📥 粘贴批量」Dialog——每行 `[买/卖] 代码 数量 价格 [手续费] [备注]`（空格/tab/逗号分隔、中英文买卖前缀、`#` 注释跳过）；解析抽离为**纯函数** `parseBatchText`（新增 `tradeV2Parse.ts` 独立模块）+ **5/5 单测**（`tradeV2Parse.test.ts`）
2. **净值曲线重构（msvvmbl0）**：新增 `NetValueChart` 组件（替换全局/分组两处重复 option）——**双 y 轴**（左金额 + 右「累计收益率%」，区间起点净值 100% 基准）+ **自定义日期范围**（起止 date 输入 + 重置）
3. **MarkdownView 柔和主题（msvvl8js）**：标题降对比（#0f172a→#1e293b/#334155）、代码块改浅色（#0f172a→#f8fafc + 边框）、行内代码柔和玫红（#9f1239）、引用/表格/分割线降对比

### 第二批（70e4d5b，5 条）

4. **交易流水跟随分组选中（msvw40lt）**：选中分组 → 流水默认过滤该组；切"全部组合"→ 重置 all（useEffect 依赖 selectedGroup?.id，不干扰手动切换）
5. **去掉「复制上一交易日」（msvw2t5y）**：删按钮 + copyPrevDay（copiedMsg 保留给粘贴导入）
6. **去掉「记一笔交易」输入交互（msvw2dub）**：删顶部「➕ 记一笔交易」+ openCreate；改「💼 去交易单」引导；编辑入口保留
7. **分组设置 tab 化（msvvra4c）**：新增「⚙️ 分组设置」tab（选中分组时）——`GroupEditor` 支持 **inline 模式**（表单内嵌 tab；弹窗保留给新建）+ **概览统计**（组内标的数/交易笔数/当前市值/占用总仓位%）；顶部按钮改切 tab
8. **知乎爬取上限 100→10000（msvvpw3a）**：service.ts `perKindLimit`/`maxTotal` 上限 + 前端 limit 输入 max

### 基础设施

- `test.mjs` 支持 **web 纯函数单测**（递归按文件名匹配 apps/web/src）
- web tsconfig 开 `allowImportingTsExtensions`（node:test + ts hook 测试 import .ts 后缀）

## 验证

- typecheck 全绿（多轮）；冒烟 18 页 ALL-PASS
- parseBatchText 单测 5/5（含逗号/注释/空文本/千分位）
- **实测 9/9**（API 造测试分组+交易 → 页面断言 → 清理）：分组设置 tab 出现/内联表单/概览统计、复制上日已去、记一笔已去、流水默认过滤当前分组、知乎 max=10000、docs 柔和主题 preBg=#f8fafc/code=#9f1239

## 遗留

- open memo 2 条：`msvwmzog`（[文档中心] 右键菜单加"复制文本"——当前 item 菜单无复制项，工具条按钮已有 copyActive 复制 md 源码）、`msvwslfq`（新增"实验"分组大需求——3 个 DeepSeek 分享链接，先 plan 再执行）
