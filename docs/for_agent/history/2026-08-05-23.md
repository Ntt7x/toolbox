# 2026-08-05-23 — 新增「改进备忘录」页面（设置分组，TODO list）

**会话主题**：设置分组新增"改进备忘录"子页面——TODO list，
用户记录使用页面过程中的小问题，开发者驱动 Agent 修复。

## 完成的功能

1. **数据层**（features/memo/store.ts）：单 KV 文档 `memo:items` → { items: MemoItem[] }
   - MemoItem：id/text/status(open|doing|done)/createdAt/updatedAt
   - 排序：open → doing → done，同状态按更新时间倒序
   - 数据源注册「改进备忘录」tag（本地数据管理页可编辑）
2. **API**：GET/POST /api/tools/memo + PUT/DELETE /api/tools/memo/:id
   （静态路由 /memo 与 /:id 路径深度不同，无 Hono 路由冲突）
3. **前端 MemoTool**（/settings/memo）：记录输入框 + 三态分组列表 +
   状态流转按钮（待处理→修复中→已完成 循环）+ 双击编辑 + 删除 + 数量徽章
4. 菜单：设置分组 staticItems 新增「改进备忘录 📝」+ Route /settings/memo

## 文件

- `features/memo/{store,index}.ts`（新）、`shared`（+Memo 契约）、`server index.ts`（装配）、
  `web/{api.ts, settings/MemoTool.tsx, App.tsx}`（菜单+路由）
- docs: history 23

## 验证

- ✅ typecheck 全绿；页面 200
- ✅ API 全链路：新增 201 → 流转 doing → 改文本 → 排序（open 在 doing 前）→ 删除 →
  数据源 memo:items（改进备忘录）

## git

本归档随改动一并提交并 push

## 遗留 / 规划

- 🔮 可加"一键复制全部待处理条目"（方便 Agent 取任务清单）
