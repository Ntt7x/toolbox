# 2026-08-05 34 · LLM 用量图表（逐日条形图 + 单日扇形图）

## 改动
- 服务端 `core/llm.ts` `getLlmUsageSummary`：byDay 每项增加当日 `byModule` 明细（{module,label,calls,totalTokens}）
- 共享契约 `LlmUsageSummary.byDay` 增加 `byModule` 字段
- 新增 `apps/web/src/components/UsageCharts.tsx`（复用 ECharts，BalanceChart 同款依赖）：
  - `DailyTokensBar`：逐日用量条形图（渐变蓝柱、tooltip 含调用次数、>31 天自动 dataZoom）
  - `DayModulePie`：单日用量环形图（中心显示当日合计 tokens、图例带 tokens、emphasis 显示百分比）
- `LlmSettings.tsx` 用量管理区块：图表区（日期下拉默认最新一天，切换刷新扇形图）

## 验证
- typecheck 全绿（server+web）
- `/api/llm/usage` byDay[0]=2026-08-05 6次 13.5k，模块明细 3 项（Chat导入 6.8k / 财报分析 6.4k / 连接测试 0.3k）
- /settings/llm 页面 200

## 分支
feat/memo-fixes（叠加在前述 memo 修复之后，待统一验收）
