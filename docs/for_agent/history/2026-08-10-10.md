# 2026-08-10-10 · 港股 Chat 补充修复（代码/名称/理由）+ 规划教训沉淀

会话主题：用户反馈 Chat 补充"未识别到可补充的个股"——港股对话（中国同辐 01763）连环修复，最终沉淀"先规划后实现"原则。

## 问题链（4 个）与修复

1. **港股代码被过滤**：模板"6 位数字、港股不用" vs normalize `/^\d{6}$/` → 港股 5 位被全过滤
   → normalize 支持 5 位港股（含前导 0 / `.HK` 后缀 / `HK` 前缀 3-5 位）统一裸 5 位
2. **LLM 稳定输出空 stocks**：单只个股对话（用户发"中国同辐"3 字），LLM 判断"非选股主题"→ 4 次复现空（模板"候选导向"+"关键行"强化后仍空）
   → **确定性兜底**：正则提取带市场后缀代码（`\d{3,6}\.(HK|SH|SZ|BJ)`）
3. **名称缺失**：正则提取名称不可靠（误匹配"港股代码/助手"）→ **confirm 时行情工具 `resolveStockName` 补名**（确定性 API，实测 01763→中国同辐）
4. **理由缺失/质量差**：兜底占位"由 Chat 对话代码提取"→ 正则从**代码后文**提取关键句（龙头/市占率等），清理代码前缀/引用标记（实测"中国核技术应用领域的龙头企业"）

## 沉淀（dev.md）

- **§2 新增「0. 先规划，后实现（硬性）」**：编程新手常见错误——规划不足急于实现，本次连环返工 6 轮；非平凡改动先写实现方案（输入→处理→输出 / 边界异常 / 兜底 / 验证路径）
- **§7.4 新增港股 Chat 补充 6 条教训**：模板与校验严格对齐 / LLM 稳定空输出需确定性兜底 / 结构化字段用确定性 API / 错误消息带 LLM 原始输出 / 新路由先 curl 验证（前端漏 `:id` 段 → 404 静默轮询 6 分钟卡死，§5.2 同源复发）/ 服务端改动重启验证

## 验证
- typecheck 全绿；watchlist 冒烟 PASS
- 完整流程实测：港股对话 preview→候选 `01763`→confirm→专题内「01763 中国同辐 | 中国核技术应用领域的龙头企业」→回滚 ✓

## Git
- `fix/watchlist-chat-append`：b3c2ba4（4 文件 +81/-5）
- 合并 main：c9dd57e → **b3c2ba4**（fast-forward，验收通过）

## 遗留 / 规划
- 🚧 分支 fix/watchlist-chat-append 合并后删除远程/本地分支
- 🔮 港股兜底理由提取是"关键词句"启发式（非 LLM 生成），多股票对话可能不精准——可后续评估"兜底后用 LLM 批量生成理由"（成本换质量，用户未要求）
