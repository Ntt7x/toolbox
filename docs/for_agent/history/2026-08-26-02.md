# 2026-08-26-02：统一数据工程（feat/data-infra-unified → main）

## 背景
用户要求"把 core/tasks 全部迁移和改造，不要混杂，用统一的数据工程"——消除双轨（core/tasks + data-infra 两套任务系统），所有分析任务走 data-infra 一条链路。随后按 P0+P1+P2 方案做代码整合优化。

## 改动
- **core/tasks 退役**（tasks.ts/sse.ts/useAsyncTask.ts 删除，createTask 全库零残留）：
  - reverseRepo 手动入口 → 统一触发调度任务（startTask("reverseRepo-monthly")，状态锁防并发）；daily 探查 → ephemeral
  - zhihuCrawler（auth/crawl/resume）→ ephemeral（onProgress 上报 SSE 进度）
  - agentSessions（chat/reasonix ask）→ ephemeral
- **统一模式整合（P0+P1+P2）**：
  - `taskResultOrError(c, taskId)` 公共 helper：9 处任务结果路由去重（统一 404/400/200 三段式）
  - 9 个业务结果路由退役（前端统一经 `GET /api/data-infra/tasks/:id` 详情读取）
  - 前端 `api.dataInfraResult<T>` + 7 页 fetchResult 一行化；api.ts 死方法（6 个）清理
  - WatchlistTool 手动轮询（import/fundamental/preview）改委托 data-infra
  - ephemeral trim 注册即裁剪（防连续运行永不触发）
- **基建强化**：ephemeral 保留上限治理（EPHEMERAL_LIMIT=100）、孤儿进度快照启动自愈、任务结构化结果/取消（cancelled 终态）/异步启动（startTask）
- **数据修复**：taskHistory 旧数据（17 条）备份清理、qAudit 测试残留清理、experiment 窗口调度任务恢复（迁移脚本误删）

## 验证
- 全量 26 测试文件全绿（data-infra 18 用例含 ephemeral 治理）；typecheck 全绿；冒烟 22 页 ALL-PASS
- API：backfill 重建快照、调度触发、健康检查 healthy=true、SSE 端点（notfound 帧）验证

## 后续
- P1-4（useAnalysisTask 包装 hook）评估后不做（fetchResult/cancel 已一行化，避免过度抽象）
- 统一模式已全部落地——后续新分析页直接套用三件套（registerTask ephemeral + startTask + taskResultOrError）
