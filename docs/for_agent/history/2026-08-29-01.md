# 2026-08-29-01 · fix/memo-hkfx 合并

## 需求
1. 港股交易流水价格显示为 HKD（00189 价格是 HKD）
2. 港股涉及遗留 CNY 符号显示为 "HK$"
3. 标的下沉页支持编辑对应标的的交易流水（与交易流水 tab 逻辑一致，编辑按钮在操作列）
4. 仓位明细标的列表隐藏"未实现%"列（CSV 导出保留全列）
5. 操作列按钮靠左显示；下沉页宽度加宽（sm:max-w-4xl → max-w-5xl）、操作列宽 w-10 → w-24

## 改动
- `apps/server/src/core/quote.ts`：港股货币判定（HKD）
- `apps/server/src/features/tradeV2/services.ts`：+53 行（交易流水编辑相关支持）
- `apps/web/src/tools/TradeV2Tool.tsx`：+55/-（操作区靠左 flex-start、下沉页加宽、行尾编辑/删除、隐藏未实现%列、HK$ 显示）
- `packages/shared/src/index.ts`：+4（类型补充）

## 经验教训
- 编辑按钮初次实现加错位置（OrderSheet 可选 prop 处直接调用 → typecheck 报 undefined）→ 操作区先确认目标组件再改
- StockHistoryDialog 解构缺 onDeleteEntry → 行尾操作区引用报未定义 → 补解构
- 脚本替换带 style 的 `e.note` 行容易匹配到多组件 → 替换前定位组件边界

## 流程
- 分支 fix/memo-hkfx（41f5e5b → 747d39d）→ 验收通过 → fast-forward 合并 main（5acdd55 → 747d39d）→ push → 删远程/本地分支
