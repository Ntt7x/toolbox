# 2026-08-10-08 · 3 条 memo（树状待办 / Chat 补充预览确认 / 个股移动复制）

会话主题：处理待办清单与专题自选股共 3 条 open memo；修复 Chat 补充改版引发的卡死。

## 完成功能

1. **待办清单树状依赖**（msp02opa）
   - TodoItem 加 `parentId`（顶层缺省）；addTodo 支持父任务（悬空引用自动降级顶层）
   - 删除父任务**级联删除全部子孙**（BFS 收集）；前端「＋/▸」按钮展开子任务输入、子任务缩进 `└` 展示、删父提示连带子任务数
   - 修复 2 bug：① `normalize` 丢弃 parentId（级联失效根因——load 后子任务失去父子关系）；② 添加子任务后误收起展开区（新子任务被隐藏）
2. **专题自选股 Chat 补充改为预览-确认**（msozzpcl）
   - 拆分 `parseImportFromChat`（提取+LLM 解析，不落库）→ `POST /:id/import/preview`（后台任务 + 候选存 KV `watchlist:importPreview:<taskId>`）→ `POST /:id/import/confirm`（勾选 codes 批量导入，用后即焚）
   - 前端「解析候选」→ 候选个股 checkbox 列表（默认全选）→「确认导入(N)」；不再自动导入
3. **专题自选股移动/复制个股**（msozy0on）
   - `POST /tools/watchlist/move-stock`（fromTopicId/code/toTopicId/copy；copy 保留源）；前端个股行「⇄」弹窗（移动/复制单选 + 目标专题下拉）
4. **Chat 补充卡死修复**：前端 `watchlistAppendPreviewStatus` 路径缺 `:id` 段（404 后 `.catch(() => null)` 静默轮询 120 次 ≈ 6 分钟）→ 补 id 参数 + 连续 5 次失败即报错退出（容忍临时网络抖动）

## 验证
- typecheck 全绿；watchlist/todo 冒烟 PASS
- 集成实测：todo 树状挂载/悬空降级/级联删除 ✓；move-stock 复制/移动/回滚 ✓；preview 路由 200（不再 404）
- playwright：todo 树父子任务添加 + 缩进显示 ✓
- 测试数据已清理（保留用户 6 条真实待办）

## Git
- `fix/memo-batch8`：b47c823（8 文件 +450/-79）
- 合并 main：7d8d8a7 → **b47c823**（fast-forward，验收通过）

## 遗留 / 规划
- 🚧 分支 fix/memo-batch8 合并后删除远程/本地分支
- 🔮 树状待办可扩展：子任务完成自动/手动联动父任务状态（暂未做，用户未要求）
