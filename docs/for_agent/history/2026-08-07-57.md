# 2026-08-07 57 · 提示词全面数据化/配置化/场景化 + 提示词管理 Tab

## 需求
把硬编码提示词进一步数据化、配置化、规范化、场景化管理；提示词管理（新增 Tab）加入 Agent 会话管理，代替代码中的硬编码。

## 实现
### 1. 场景化元数据（core/prompts.ts）
- `PROMPT_META`：id → [场景分组, 归属页面]（交易 13 / 知识库 5）；导出 `promptGroup`/`promptPage`
- `listPrompts()` 输出含 group/page；`PromptMeta`（shared）加 group/page 字段
- 修 grid-plan 漏配（meta 误写 grid-plan.prompt → grid-plan.system）

### 2. knowledgeSession 硬编码迁移
- 新增模板：`knowledge.agent.guide`（会话引导词 {instance} {action}）、`knowledge.agent.ask`（问答指令 {instance} {question}）、`knowledge.agent.import`（导入指令 {instance} {dialog}）
- guideFor/doAsk/doImport 改用 getPromptTemplate 渲染（占位符 replace）

### 3. 前端提示词管理 Tab（Agent 会话管理页）
- 第三个 Tab「📝 提示词管理」：按场景分组（details 折叠）+ 每项 id/page/description
- 编辑（textarea 保存即时生效 settings:prompt.*）、👁 预览（默认参数渲染）、↺ 重置
- api 复用既有 prompts/promptDetail/promptUpdate/promptReset

### 4. 测试与文档
- 新增 prompts.test.ts 4 个：注册表分组完整性（无"通用"漏配）、knowledge.agent 渲染、update/reset、promptGroup/Page
- dev.md §4.4 强化提示词管理规范（新提示词必须登记 PROMPT_META，否则落"通用"被测试拦）

## 验证
- typecheck 全绿；单测 76/76（+4）
- API：/api/prompts 18 个提示词，场景分组 交易13 + 知识库5（无通用漏配）；knowledge.agent.guide 正常
- 页面 /settings/agent-sessions 200

## 分支
feat/prompt-manage（待验收）
