# 2026-08-24-01 · 净值曲线收益率双口径（净值 TWR / 最大成本）

分支 `fix/return-metrics`（aa95064，3 文件 +134/-21）→ 验收通过 → fast-forward 合并 main。

## 改动总结

### 收益率计算改为行业两种标准（用户提供公式，未自行调研）
- **净值收益率（默认，时间加权 TWR）**：`NAVᵢ = 期初本金 p₀ + 市值ᵢ − 累计净投入ᵢ`；曲线 `NAVᵢ/NAV₀ − 1`（∏(1+日收益率)−1 = NAV末/NAV初−1，与 TWR 等价）
- **最大成本收益率（可切换）**：`累计收益(NAVᵢ−p₀) ÷ 历史最大净投入 C_max`（保守口径）
- UI：净值曲线右上角口径切换（默认净值），图例名随口径变化，提示行随口径显示公式说明
- 文档：`docs/for_agent/domains/return-metrics.md`（完整公式 + 举例 + 区别表），dev.md 已加引用

## 验证
- typecheck 全绿；冒烟 PASS
- playwright 实测：默认净值按钮 + 切换后提示变"历史最大净投入成本"，无错误
